# InCast Ingestion Tool Configuration
# Copy this file to config.yaml and update with your values
# Environment variables take precedence over this file

# API Configuration
api:
  backend_url: "https://api-dev.incast.ai"
  publish_url: "https://api-dev.incast.ai/publish-comment"
  jwt_token: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJlbWFpbCI6InJ1ZGVua295ZWhvcjg5MUBnbWFpbC5jb20iLCJleHAiOjE3NjM1OTIzOTAsIm9yaWdJYXQiOjE3NjM1ODg3OTAsInVzZXJfaWQiOiIyYmM0ZDgyYy1hZGQ3LTRiOTAtYjExNS00OGUzZTllYzQwOWUifQ.ntIin3_Qjo2mOc_cfNOdczI04TbJrY29KFrxQatYH30"

# Processing Configuration
processing:
  list_file: "list.txt"
  max_comments: 100
  rate_limit: 0.5  # Seconds between comment API calls
  max_retries: 3  # API call retry attempts
  retry_delay: 1.0  # Initial retry delay in seconds
  max_items_limit: "2"  # Limit items to process: "all" or number (e.g., 10, 50, 100). In dry_run mode defaults to 10.

# Mode Configuration
modes:
  dry_run: false # Test mode without actual uploads
  video_only: false  # Upload videos only, skip comments
  comments_only: false  # Import comments only (requires asset_id in config)
  asset_id: "f33d70f2-e1a6-4d53-8d66-3a0d58143e01"  # Asset ID to use in comments_only mode

# Logging Configuration
logging:
  level: "INFO"  # DEBUG, INFO, WARNING, ERROR, CRITICAL
  log_file: ""  # Optional log file path (empty = console only)
  verbose: false

# Cache Configuration
cache:
  enabled: false
  cleanup_after_days: 30  # Clean up cache files older than this